import { MarqueeImageDirective } from './marquee-image.directive';

describe('MarqueeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MarqueeImageDirective();
    expect(directive).toBeTruthy();
  });
});
